"use client";

import OrderDetailModernWorkspace from "@/components/OrderDetailModernWorkspace";

export default function OrderDetailWorkspace({ orderId }: { orderId: string }) {
  return <OrderDetailModernWorkspace orderId={orderId} />;
}
